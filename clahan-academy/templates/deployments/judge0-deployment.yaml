apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "clahan.fullname" . }}-judge0
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "clahan.labels" . | nindent 4 }}
    app.kubernetes.io/component: judge0
    role: ai
spec:
  replicas: {{ .Values.judge0.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "clahan.name" . }}-judge0
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "clahan.name" . }}-judge0
        app.kubernetes.io/instance: {{ .Release.Name }}
        role: ai
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "clahan.fullname" . }}-judge0
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000

      containers:
        - name: judge0
          {{- if .Values.global.imageRegistry }}
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.judge0.image.repository }}:{{ .Values.judge0.image.tag }}"
          {{- else }}
          image: "{{ .Values.judge0.image.repository }}:{{ .Values.judge0.image.tag }}"
          {{- end }}
          imagePullPolicy: {{ .Values.judge0.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 2358
              protocol: TCP
          
          resources:
            {{- toYaml .Values.judge0.resources | nindent 12 }}
          
          securityContext:
            readOnlyRootFilesystem: {{ .Values.judge0.securityContext.readOnlyRootFilesystem }}
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          
          livenessProbe:
            httpGet:
              path: {{ .Values.judge0.probes.liveness.path }}
              port: 2358
            initialDelaySeconds: {{ .Values.judge0.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.judge0.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.judge0.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.judge0.probes.liveness.failureThreshold }}
          
          readinessProbe:
            httpGet:
              path: {{ .Values.judge0.probes.readiness.path }}
              port: 2358
            initialDelaySeconds: {{ .Values.judge0.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.judge0.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.judge0.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.judge0.probes.readiness.failureThreshold }}
          
          envFrom:
            - configMapRef:
                name: {{ include "clahan.fullname" . }}-judge0

          volumeMounts:
            - name: tmp-dir
              mountPath: /tmp

      volumes:
        - name: tmp-dir
          emptyDir: {}

      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - {{ include "clahan.name" . }}-judge0
                topologyKey: kubernetes.io/hostname
      
      nodeSelector:
        {{- toYaml .Values.judge0.nodeSelector | nindent 8 }}
      
      tolerations:
        {{- toYaml .Values.judge0.tolerations | nindent 8 }}
