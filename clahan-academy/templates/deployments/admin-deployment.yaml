apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "clahan.fullname" . }}-admin
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "clahan.labels" . | nindent 4 }}
    app.kubernetes.io/component: admin
    role: app
spec:
  replicas: {{ .Values.admin.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "clahan.name" . }}-admin
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "clahan.name" . }}-admin
        app.kubernetes.io/instance: {{ .Release.Name }}
        role: app
        azure.workload.identity/use: "true"
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "clahan.fullname" . }}-admin
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000

      containers:
        - name: admin
          {{- if .Values.global.imageRegistry }}
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.admin.image.repository }}:{{ .Values.admin.image.tag }}"
          {{- else }}
          image: "{{ .Values.admin.image.repository }}:{{ .Values.admin.image.tag }}"
          {{- end }}
          imagePullPolicy: {{ .Values.admin.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 4002
              protocol: TCP
          
          resources:
            {{- toYaml .Values.admin.resources | nindent 12 }}
          
          securityContext:
            readOnlyRootFilesystem: {{ .Values.admin.securityContext.readOnlyRootFilesystem }}
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          
          livenessProbe:
            httpGet:
              path: {{ .Values.admin.probes.liveness.path }}
              port: 4002
            initialDelaySeconds: {{ .Values.admin.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.admin.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.admin.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.admin.probes.liveness.failureThreshold }}
          
          readinessProbe:
            httpGet:
              path: {{ .Values.admin.probes.readiness.path }}
              port: 4002
            initialDelaySeconds: {{ .Values.admin.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.admin.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.admin.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.admin.probes.readiness.failureThreshold }}
          
          envFrom:
            - configMapRef:
                name: {{ include "clahan.fullname" . }}-admin
            - secretRef:
                name: {{ include "clahan.fullname" . }}-admin-secret

          volumeMounts:
            - name: tmp-dir
              mountPath: /tmp
            - name: secrets-store-inline
              mountPath: "/mnt/secrets"
              readOnly: true

      volumes:
        - name: tmp-dir
          emptyDir: {}
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "{{ include "clahan.fullname" . }}-admin-spc"

      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - {{ include "clahan.name" . }}-admin
                topologyKey: kubernetes.io/hostname
      
      nodeSelector:
        {{- toYaml .Values.admin.nodeSelector | nindent 8 }}
      
      tolerations:
        {{- toYaml .Values.admin.tolerations | nindent 8 }}
